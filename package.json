{
  "name": "resuma",
  "version": "0.1.0",
  "private": true,
  "description": "Public website and documentation for Resuma.",
  "scripts": {
    "serve": "python3 -m http.server 4173 --directory site",
    "serve:docs": "python3 -m http.server 4174 --directory docs",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
